import { prettyNumber } from '../../../helpers/pretty-number';
import { module, test } from 'qunit';

module('Unit | Helper | pretty number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = prettyNumber(42);
  assert.ok(result);
});
